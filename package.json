{
  "name": "guac",
  "private": true,
  "packageManager": "npm@11.11.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "if [ \"$SERVICE_TYPE\" = \"web\" ]; then cd apps/web && npx next start -p ${PORT:-3000}; else cd apps/api && npx tsx src/env.ts; fi",
    "test": "turbo test",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  }
}
